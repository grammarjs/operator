
/**
 * Expose `plugin`.
 */

module.exports = plugin;

/**
 * Define general operators to use in other grammars.
 */

function plugin(opts) {
  return function(grammar){
    var rule = grammar.rule;

  };
}